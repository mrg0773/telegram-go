---
name: qa-checklist
description: Чек-лист тестировщика для @mrg0773. Используй перед мержем, после написания кода, для code review.
allowed-tools: Read, Grep, Glob, Bash
---

# QA Checklist - Проверки перед мержем

## 🔴 Критические проверки

### 1. Типы TypeScript
```bash
npm run typecheck
# Должно быть 0 ошибок
```

### 2. Тесты
```bash
npm run test
# Все тесты должны проходить
```

### 3. Линтер
```bash
npm run lint
# 0 ошибок, warnings допустимы
```

### 4. Сборка
```bash
npm run build
# Успешная сборка без ошибок
```

## 🟡 Проверки кода

### Обработка ошибок
- [ ] Используется `Result` из neverthrow
- [ ] Нет голых `throw`
- [ ] Все ошибки типизированы через `AppError`
- [ ] Логируются через `@mrg0773/logger-error`

### Типизация
- [ ] Нет `any` (кроме обоснованных случаев)
- [ ] Все параметры функций типизированы
- [ ] Все возвращаемые значения типизированы
- [ ] Используются строгие типы (не `string`, а `UserId`)

### Безопасность
- [ ] Нет хардкода секретов
- [ ] Валидация входных данных
- [ ] SQL injection защита (параметризованные запросы)
- [ ] Нет eval/Function конструкторов

### Логирование
- [ ] Нет `console.log` в продакшен коде
- [ ] Используется `log` из `@mrg0773/logger-error`
- [ ] Логируются важные операции
- [ ] Нет чувствительных данных в логах

## 🟢 Проверки качества

### Код
- [ ] Функции не больше 50 строк
- [ ] Нет дублирования кода
- [ ] Понятные имена переменных и функций
- [ ] Нет закомментированного кода

### Тесты
- [ ] Покрытие > 80%
- [ ] Тестируются edge cases
- [ ] Тестируются ошибки
- [ ] Моки для внешних зависимостей

### Документация
- [ ] README актуален
- [ ] JSDoc для публичных функций
- [ ] CHANGELOG обновлён (для библиотек)

## Автоматические проверки

### Запустить все проверки
```bash
npm run typecheck && npm run lint && npm run test && npm run build
```

### Проверить coverage
```bash
npm run test:coverage
```

### Найти console.log
```bash
grep -r "console\." src/ --include="*.ts" | grep -v "// allowed"
```

### Найти any
```bash
grep -r ": any" src/ --include="*.ts" | grep -v "// TODO"
```

### Найти throw
```bash
grep -r "throw " src/ --include="*.ts"
```

## Что делать при ошибках

### TypeScript ошибки
1. Прочитай сообщение ошибки
2. Исправь типы
3. Не используй `as any` как workaround

### Тесты падают
1. Прочитай assertion
2. Проверь fixtures
3. Проверь моки
4. Исправь код или тест

### Линтер ругается
1. Прочитай правило
2. Исправь код по правилу
3. Если правило неверно - создай issue

## После проверок

1. **Все зелёное** → Можно мержить
2. **Есть ошибки** → Исправить, перезапустить
3. **Непонятные ошибки** → Создать issue, не мержить

## Чек-лист для Code Review

- [ ] Код понятен без объяснений
- [ ] Нет очевидных багов
- [ ] Нет security issues
- [ ] Соответствует архитектуре проекта
- [ ] Тесты адекватные
- [ ] Нет лишних изменений
