---
name: dev-workflow
description: Правила работы программиста в @mrg0773. Используй когда находишь баги, нужно создать issue, или есть вопросы по workflow.
allowed-tools: Read, Grep, Glob, Bash
---

# Dev Workflow - Правила работы программиста

## 🚨 ГЛАВНОЕ ПРАВИЛО: Ошибки в библиотеках

**НЕЛЬЗЯ обходить ошибки workaround-ами!**

Если нашёл баг в @mrg0773 библиотеке:

1. **Создай issue** в репозитории библиотеки
2. **Дождись исправления** - не пиши костыли
3. **Обнови зависимость** после фикса

### Почему это важно

- Workaround-ы накапливаются и ломают код
- Другие разработчики наступят на те же грабли
- Баг останется в либе навсегда

### Как создать issue

```bash
# Найти project_id
curl -s -H "PRIVATE-TOKEN: $GITLAB_TOKEN" \
  "https://git.tgfermagames.ru/api/v4/projects?search=LIBRARY_NAME" | jq '.[0].id'

# Создать issue
curl -X POST \
  -H "PRIVATE-TOKEN: $GITLAB_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Bug: краткое описание",
    "description": "## Проблема\n\n## Шаги воспроизведения\n\n## Ожидаемое поведение\n\n## Фактическое поведение",
    "labels": "bug"
  }' \
  "https://git.tgfermagames.ru/api/v4/projects/{PROJECT_ID}/issues"
```

## Project IDs основных библиотек

| ID | Библиотека | Область |
|----|------------|---------|
| 47 | logger-error | Логирование |
| 85 | trace-lib | Трейсинг |
| 77 | ydb | База данных |
| 87 | telegram-lib | Telegram API |
| 62 | tgferma | Бизнес-логика |
| 69 | users | Пользователи |
| 63 | schemas | JSON схемы |

## Workflow разработки

### 1. Перед началом работы

```bash
# Обновить зависимости
npm update

# Проверить актуальность main
git fetch origin
git status
```

### 2. Во время разработки

- Пиши код, следуя стандартам проекта
- Используй `@mrg0773/logger-error` для логов
- Используй `neverthrow` для обработки ошибок
- Не добавляй console.log

### 3. Перед коммитом

```bash
# Проверить типы
npm run typecheck

# Запустить тесты
npm run test

# Проверить линтер
npm run lint
```

### 4. Коммит и пуш

```bash
# Конвенция коммитов
git commit -m "feat: добавлен функционал X"
git commit -m "fix: исправлен баг Y"
git commit -m "refactor: рефакторинг Z"

# Пуш
git push origin main
```

## Что делать если...

### Нашёл баг в своём коде
→ Исправь и закоммить

### Нашёл баг в @mrg0773 библиотеке
→ **Создай issue**, дождись фикса, обнови зависимость

### Нужна новая фича в библиотеке
→ Создай issue с меткой `feature`

### CI упал
→ Проверь логи, исправь, перезапусти pipeline

### Не понимаю как работает библиотека
→ Читай README и исходники, создай issue с меткой `question`

## 🚨 После решения issue - ВСЕГДА коммит и пуш!

Когда issue решено:
1. **Сразу коммит** с описанием что исправлено
2. **Сразу пуш** в remote
3. **Закрыть issue** в GitLab

```bash
git add .
git commit -m "fix: описание исправления

Closes #123"
git push origin main
```

**НЕ откладывать!** Незапушенный код = несуществующий код.

## 🚨 Деплой только через CI/CD!

**ЗАПРЕЩЕНО делать деплой вручную!**

Все деплои происходят автоматически через CI/CD pipeline:
1. Коммит изменений
2. Пуш в main
3. CI/CD автоматически деплоит

```bash
# ПРАВИЛЬНО - деплой через CI
git add .
git commit -m "feat: новая фича"
git push origin main
# CI сам задеплоит

# НЕПРАВИЛЬНО - ручной деплой
sf deploy          # ❌ ЗАПРЕЩЕНО
yc serverless ...  # ❌ ЗАПРЕЩЕНО
npm run deploy     # ❌ ЗАПРЕЩЕНО
```

### Почему нельзя деплоить вручную

- Нет версионирования изменений
- Невозможно откатить
- Другие не знают что задеплоено
- Конфликты с CI/CD
- Потеря истории изменений

## Запрещено

❌ Копировать код из библиотек в проект
❌ Писать workaround-ы для багов в либах
❌ Игнорировать ошибки TypeScript
❌ Коммитить с `--no-verify`
❌ Пушить в main без тестов
❌ Оставлять код без коммита после решения issue
❌ Делать деплой вручную (только через git push → CI/CD)
